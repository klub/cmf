# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi
source /home/{{ lookup('env', 'SUDO_USER') }}/env

# User specific environment and startup programs

PATH=$PATH:$HOME/bin

export PATH
